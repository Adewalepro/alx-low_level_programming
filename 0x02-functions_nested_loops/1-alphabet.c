#include "main.h"
#include <stdio.h>
/**
*main - a function that prints the alphabet, in lowercase
*Description: alphabet in lowercase
*
*
*Return 0 always
*/
int main(void)
{ char letter;
for (letter = 'a'; let <= 'z'; letter++)
putchar(letter);
putchar('\n');
return (0); i}
