#include <stdio.h>
/**
*main - a function that prints the alphabet, in lowercase
*Description: alphabet in lowercase
*
*
*Return 0 always
*/
int main(void)
{ char let;
for (let = 'a' let <= 'z' let++)
{ putchar(let); }
putchar('\n');
return (0); }
